{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "CareVantageConnection": "Host=${POSTGRES_HOST};Port=${POSTGRES_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}",
    "KeycloakConnection": "Host=${POSTGRES_HOST};Port=${POSTGRES_KEYCLOAK_PORT};Database=${POSTGRES_DB_KEYCLOAK};Username=${POSTGRES_KEYCLOAK_USER};Password=${POSTGRES_KEYCLOAK_PASSWORD}"
  }
}